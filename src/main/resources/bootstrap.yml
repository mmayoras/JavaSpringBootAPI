spring:
  application:
    name: ps-hamanagementapi
  cloud:
     config:
       enabled: true
       uri: "https://ci-configserver.apps-np.homedepot.com/"

#Default

lifecycle: default
auth_enforceFilter: false
auth_enforceTimeout: false
enforceDuplicateCheck: true
auth_key: b9b640396e23443980af4a2599222169
auth_secret: Timestamp
uri: "https://ci-configserver.apps-np.homedepot.com/"



# Development
---
spring.profiles: development

lifecycle: development
auth_enforceFilter: false
auth_enforceTimeout: false

spring:
  cloud:
    config:
      uri: "https://ci-configserver.apps-np.homedepot.com/"
      discovery:
        enabled: false


# QA
---
spring.profiles: acceptance

lifecycle: acceptance
auth_enforceFilter: true
auth_enforceTimeout: false

spring:
  cloud:
    config:
      uri: "https://ci-configserver.apps-np.homedepot.com/"
      discovery:
        enabled: false

# Production
---
spring.profiles: production

lifecycle: production
auth_enforceFilter: true
auth_enforceTimeout: true

spring:
  cloud:
    config:
      uri: "https://ci-configserver.apps.homedepot.com/"
      discovery:
        enabled: false
